#!/user/bin/env perl -w

use v5.30;
# use warnings;
# use diagnostics;

# use File::Basename qw/ basename /;

# my $name = `which perl`;
# (my $basename = $name) =~ s#.*/##;
# my $basename = $name =~ s#.*/##r;
# print $basename;

# my $name = `which perl`;
# my $basename = basename $name;
# print $basename;

# use File::Basename qw/ /;
# my $name = `which perl`;
# my $dirname = File::Basename::dirname $name;
# print $dirname;

# use File::Spec;
# use File::Basename;

# print "Please enter a filename: ";
# chomp(my $old_name = <STDIN>);
#
# my $dirname = dirname $old_name;
# my $basename = dirname $old_name;
#
# say "dir = $dirname, base = $basename";
# # $basename =~ s/^/not/;
# # my $new_name = "$dirname/$basename";
#
# my $new_name = File::Spec->catfile($dirname, $basename);
#
# rename($old_name, $new_name)
#     or warn "Can't rename '$old_name' to '$new_name': $!";
#

# use Path::Class;
#
# my $dir = dir(qw(Users fred lib));
# say $dir;
# my $subdir = $dir->subdir('perl5');
# say $subdir;
# my $parent = $dir->parent;
# say $parent;
#
# my $windir = $dir->as_foreign('Win32');
# say $windir;

# use Time::Moment;
#
# my $dt = Time::Moment->from_epoch(time);
# say $dt;


